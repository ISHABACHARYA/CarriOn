import React, {Component} from 'react';
import {
    View,
    Text,
    TextInput,
} from 'react-native';

export default class InputField extends Component{
    render(){
        const {label, placeholder, placeholderColor, secureTextEntry,}
        return(
            <View>
                <TextInput 
                
                
                />
            </View>
        )
    }
}